[2026-07-14]
- Created CodexModify.txt to record approved modifications made by Codex in this folder.
- Updated requirements.txt after user approval:
  - Added selenium==4.45.0 for crawlers.py Selenium browser automation.
  - Added fake-useragent==2.2.0 for crawlers.py user-agent generation.
  - Added openai==2.36.0 for Learning/testLangChain.py OpenAI import.
  - Added pymongo==4.17.0 for Learning/testLangChain.py MongoDB client usage.
  - Added langchain-mongodb==0.11.0 for Learning/testLangChain.py MongoDB Atlas vector search usage.

[2026-08-26]
- Updated crawlers.py after user approval:
  - Changed crawler persistence from one final full-run insert to one insert after each dataset/department completes.
  - Added per-dataset result accumulation with dataset_results to avoid losing previous departments if a later browser step fails.
  - Initialized symptom_list_select before applying SYMPTOM_LIMIT so departments with 50 or fewer symptoms do not raise an undefined-variable error.

[2026-09-03]
- Updated main.py after user approval:
  - Replaced the supported-departments caption with a clickable sidebar text button.
  - Added a Streamlit dialog titled "當前支援詢問的科別" listing the currently supported departments.
  - Added sidebar button hover styling so the text presents as clickable.
  - Updated the sidebar clickable text color to white and hover color to gray.

[2026-09-10]
- Added Fly.io deployment support:
  - Added Dockerfile using python:3.14.3-slim and Streamlit startup command for main.py on port 8080.
  - Added fly.toml with Dockerfile build settings, Tokyo/Narita primary region, HTTPS HTTP service, and autoscaling-to-zero defaults.
  - Added .dockerignore to keep local virtualenvs, logs, Git metadata, and local secrets out of the Docker build context.
  - Updated requirements.txt with streamlit==1.63.0 for the Streamlit entrypoint and dnspython==2.8.0 for mongodb+srv DNS resolution.
  - Updated utils.py and chains.py so secrets can be read from Fly.io environment variables or local Streamlit secrets.
