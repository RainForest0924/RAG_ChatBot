[2026-07-14]
- Created CodexModify.txt to record approved modifications made by Codex in this folder.
- Updated requirements.txt after user approval:
  - Added selenium==4.45.0 for crawlers.py Selenium browser automation.
  - Added fake-useragent==2.2.0 for crawlers.py user-agent generation.
  - Added openai==2.36.0 for Learning/testLangChain.py OpenAI import.
  - Added pymongo==4.17.0 for Learning/testLangChain.py MongoDB client usage.
  - Added langchain-mongodb==0.11.0 for Learning/testLangChain.py MongoDB Atlas vector search usage.

[2026-08-26]
- Updated crawlers.py after user approval:
  - Changed crawler persistence from one final full-run insert to one insert after each dataset/department completes.
  - Added per-dataset result accumulation with dataset_results to avoid losing previous departments if a later browser step fails.
  - Initialized symptom_list_select before applying SYMPTOM_LIMIT so departments with 50 or fewer symptoms do not raise an undefined-variable error.

[2026-09-03]
- Updated main.py after user approval:
  - Replaced the supported-departments caption with a clickable sidebar text button.
  - Added a Streamlit dialog titled "當前支援詢問的科別" listing the currently supported departments.
  - Added sidebar button hover styling so the text presents as clickable.
  - Updated the sidebar clickable text color to white and hover color to gray.

[2026-09-10]
- Added Fly.io deployment support:
  - Added Dockerfile using python:3.14.3-slim and Streamlit startup command for main.py on port 8080.
  - Added fly.toml with Dockerfile build settings, Tokyo/Narita primary region, HTTPS HTTP service, and autoscaling-to-zero defaults.
  - Added .dockerignore to keep local virtualenvs, logs, Git metadata, and local secrets out of the Docker build context.
  - Updated requirements.txt with streamlit==1.63.0 for the Streamlit entrypoint and dnspython==2.8.0 for mongodb+srv DNS resolution.
  - Updated utils.py and chains.py so secrets can be read from Fly.io environment variables or local Streamlit secrets.

[2026-09-14]
- Updated .github/workflows/fly-deploy.yml for Fly.io GitHub Actions deployment:
  - Updated the Fly.io continuous deployment documentation link.
  - Added manual workflow_dispatch support.
  - Added read-only repository permissions.
  - Added FLY_APP environment variable for rag-chatbot-wild-tidepool-9132.
  - Changed deploy command to pass explicit --config fly.toml and --app "$FLY_APP".
  - Replaced the simple concurrency string with a branch-scoped concurrency group and cancel-in-progress.
  - Added step names and a 30-minute deploy timeout.

[2026-09-15]
- Added specification.md for the requested Streamlit login feature before implementation:
  - Documented login screen components, authentication flow, lockout behavior, demo admin account, secrets configuration, session state, constraints, and acceptance criteria.
- Updated main.py for the demo admin login flow:
  - Added Streamlit session-state initialization for authenticated, failed_login_attempts, login_lockout_until, show_password, and history.
  - Added Argon2 password verification against ADMIN_USERNAME and ADMIN_PASSWORD_HASH from project secret loading.
  - Added login screen with account input, password input, password visibility toggle, login button, incorrect-credential error, and failed-login lockout.
  - Wrapped the existing chatbot UI in render_chatbot() so unauthenticated users only see the login screen.
  - Set temporary local/test lockout duration to 60 seconds per request.
  - Fixed lockout remaining-time logic so failed attempts are not reset before an actual lockout expires.
  - Added login_error_message session state and reruns after failed attempts so the disabled login button appears immediately after the fifth consecutive failure.
  - Used utils.get_secret() for admin credentials so local .streamlit/secrets.toml and Fly.io environment secrets are both supported.
- Updated .streamlit/secrets.toml:
  - Added ADMIN_USERNAME for ChatBotAdmin.
  - Added ADMIN_PASSWORD_HASH as an Argon2 hash for Test123456.
- Updated requirements.txt:
  - Added argon2-cffi==25.1.0 for Argon2 hash verification.
- Local verification completed:
  - Ran python -m py_compile main.py utils.py chains.py successfully.
  - Verified ADMIN_PASSWORD_HASH validates Test123456 with Argon2.
  - Verified initial login screen, successful admin login, invalid-credential error, password visibility toggle, five-failure lockout, and lockout expiry recovery with Streamlit AppTest.
  - Started python -m streamlit run main.py --server.headless true --server.port 8501 and confirmed http://localhost:8501 returned HTTP 200.
